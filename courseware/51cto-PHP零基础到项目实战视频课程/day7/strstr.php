<?php
	$str = "test@ghost.com.cn";
	// echo strstr( $str, "." );
	// echo strstr( $str, ".", true );

	// echo strpos( $str, "." );
	// echo strpos( $str, ".", 11 );

	// echo strrpos( $str, "." );

	// echo substr( $str, 0, 5 );
	// echo substr( $str, -1 );
	// echo substr( $str, -2 );
	// echo substr( $str, 0, -2 );
	echo substr( $str, -3, -1 );
?>